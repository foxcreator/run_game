# 🛒 Інтеграція Магазину з Вертушкою

## 📋 Огляд

Магазин тепер повністю функціональний! Гравці можуть купувати **Вертушки** за зароблені долари.

---

## 🎯 Як це працює

### 1. Заробіток грошей в грі
- Гравець збирає гривні (10₴, 20₴, 50₴, 100₴, 200₴, 500₴)
- Обмінює їх на долари в обмінниках (43₴ = $1)
- Долари зберігаються в "банку"

### 2. Магазин
- Доступний з екрану результатів (кнопка "МАГАЗИН")
- Показує поточний баланс
- Показує кількість вертушок
- Дозволяє купити вертушки за $50 кожна

### 3. Використання вертушок
- Активація: клавіша **E**
- Відкидає та заморожує всіх ворогів в радіусі
- Cooldown: 45 секунд між використаннями
- Кількість використань = кількість куплених вертушок

---

## 🔧 Технічна реалізація

### SaveSystem
```javascript
// Нові методи для збереження вертушок
getSpinnerCount()      // Отримати кількість вертушок
setSpinnerCount(count) // Встановити кількість
addSpinnerCount(amount) // Додати вертушки
```

### ShopScene
```javascript
// Ініціалізація
init() {
    this.saveSystem = new SaveSystem();
    this.bankedMoney = this.saveSystem.getBankedMoney();
    this.spinnerCount = this.saveSystem.getSpinnerCount();
}

// Купівля вертушки
buySpinner() {
    if (this.bankedMoney >= price) {
        this.bankedMoney -= price;
        this.spinnerCount++;
        this.saveSystem.setBankedMoney(this.bankedMoney);
        this.saveSystem.addSpinnerCount(1);
    }
}
```

### SpinnerBonus
```javascript
// Конструктор тепер приймає SaveSystem
constructor(scene, player, saveSystem) {
    this.saveSystem = saveSystem;
    this.usesLeft = saveSystem.getSpinnerCount();
}

// При використанні
activate() {
    this.usesLeft--;
    this.saveSystem.setSpinnerCount(this.usesLeft);
}
```

### GameScene
```javascript
// Ініціалізація з SaveSystem
this.spinnerBonus = new SpinnerBonus(this, this.player, this.saveSystem);
```

---

## 🎨 UI Елементи

### Магазин (ShopScene)
- **Баланс:** `💰 Баланс: $150`
- **Кількість вертушок:** `🌀 Вертушки: 3`
- **Кнопка купівлі:** `КУПИТИ ВЕРТУШКУ - $50`
- **Опис:** Текст з поясненням що робить вертушка

### HUD (в грі)
- **Немає вертушок:** `🌀 Вертушка: НЕМАЄ (купи в магазині)` (червоний)
- **На cooldown:** `🌀 Вертушка: 15с (x3)` (білий)
- **Готова:** `🌀 Вертушка: ГОТОВА (E) x3` (зелений)

---

## 📊 Конфігурація

```javascript
// src/config/gameConfig.js
SPINNER_BONUS: {
    KEY: 'KeyE',
    COOLDOWN: 45000,         // 45 секунд
    PUSH_FORCE: 300,
    FREEZE_DURATION: 4000,   // 4 секунди
    RADIUS: 150,
    SHOP_PRICE: 50,          // Ціна в магазині ($)
    INITIAL_COUNT: 0,        // Початкова кількість
    MESSAGES: {
        ACTIVATED: 'ВЕРТУШКА! Вороги відкинуті!',
        COOLDOWN: 'Вертушка ще не готова. Зачекай {seconds} сек.',
        NO_ENEMIES: 'Поблизу немає ворогів.',
        NO_USES: 'У тебе немає вертушок! Купи в магазині.'
    }
}
```

---

## 🎮 Геймплей

### Цикл гри
1. **Грай** → Збирай гривні
2. **Обмінюй** → Конвертуй в долари
3. **Програй** → Перейди в ResultScene
4. **Магазин** → Купи вертушки
5. **Грай знову** → Використовуй вертушки (E)

### Стратегія
- Вертушки коштують дорого ($50)
- Використовуй їх в критичних ситуаціях
- Cooldown 45 секунд - плануй використання
- Купуй декілька вертушок наперед

---

## 🔄 Збереження

Всі дані зберігаються в `localStorage`:
```javascript
{
    bankMoney: 150,      // Баланс в доларах
    spinnerCount: 3,     // Кількість вертушок
    bestScore: 1000,
    // ... інші дані
}
```

---

## ✅ Переваги реалізації

1. **Персистентність** - вертушки зберігаються між сесіями
2. **Економіка** - гравець має мотивацію заробляти гроші
3. **Стратегія** - треба вибирати коли використовувати
4. **Прогресія** - можна накопичувати вертушки
5. **Feedback** - чіткі повідомлення про стан

---

## 🚀 Майбутні покращення

- Додати більше предметів в магазин
- Додати знижки/акції
- Додати досягнення за покупки
- Додати візуальну анімацію покупки
- Додати звуки для магазину

---

**Готово до тестування!** 🎉
