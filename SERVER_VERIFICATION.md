# 🕵️‍♂️ Як перевірити сервер після деплою

Цей файл містить команди для ручної перевірки налаштувань сервера.

## 1. Підключення до сервера
```bash
ssh root@46.62.155.14 -i infrastructure/ansible-playbooks/hetzner_key
```

## 2. Перевірка компонентів

### ✅ Node.js
Має бути версія 20.x:
```bash
node -v
npm -v
```

### ✅ MySQL
Перевірка статусу сервісу:
```bash
systemctl status mysql
```
Перевірка входу в базу (пароль має братись з `.my.cnf`, тому вводити його не треба):
```bash
mysql -e "SHOW DATABASES;"
```
Має показати бази `game` та `sys`, `mysql` тощо.

### ✅ Nginx
Перевірка статусу та конфігурації:
```bash
systemctl status nginx
nginx -t
```

### ✅ GitHub Runner
Перевірка, чи працює сервіс ранера:
```bash
systemctl status actions.runner.*
```
(Назва сервісу може трохи відрізнятись, але вона починається на `actions.runner`).

Також можна глянути чи юзер `runner` існує:
```bash
id runner
```

### ✅ Проект та права
Перевірка папок:
```bash
ls -la /var/www/busification-run
ls -la /var/www/busification-run/server
```
Власником має бути `runner:runner`.

Перевірка `.env` файлу (тільки чи існує):
```bash
ls -l /var/www/busification-run/server/.env
```

### ✅ PM2 (Process Manager)
Перевірка процесів (поки що може бути порожньо, якщо деплой ще не пройшов):
```bash
pm2 status
```
Або, якщо PM2 запущено від юзера `runner`:
```bash
sudo -u runner pm2 status
```

---

## 3. Запуск деплою (First Run)
Якщо все ок, на локальній машині запускаємо Ansible:
```bash
# В папці infrastructure/ansible-playbooks
ansible-playbook -i inventory/production/game game.yml --ask-vault-pass
```
(Треба ввести пароль, яким шифрували `vars/default.yml`. Якщо не шифрували — прибрати `--ask-vault-pass`).
