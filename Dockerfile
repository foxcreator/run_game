# Використовуємо легкий Nginx образ для статичного контенту
FROM nginx:alpine

# Копіюємо файли проєкту в директорію Nginx
COPY . /usr/share/nginx/html

# Nginx за замовчуванням слухає на порту 80
EXPOSE 80

# Nginx запускається автоматично в базовому образі