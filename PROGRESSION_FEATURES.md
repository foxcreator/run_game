# 🎮 Нові Механіки Прогресії та Різноманітності

## 📋 Огляд

Цей документ описує нові системи, додані до гри для покращення прогресії та різноманітності геймплею.

---

## ✨ Реалізовані Механіки

### 1️⃣ Система Повідомлень (NotificationManager)

**Файл:** `src/systems/NotificationManager.js`

**Опис:** Централізована система для відображення всіх повідомлень в грі.

**Функції:**
- Черга повідомлень з пріоритетами
- Автоматичне зникнення
- Плавна анімація появи/зникнення
- Підтримка до 5 повідомлень в черзі

**Використання:**
```javascript
this.notificationManager.show(
    'Текст повідомлення',
    GAME_CONFIG.NOTIFICATIONS.PRIORITY.HIGH,
    4000 // тривалість (мс)
);
```

---

### 2️⃣ Система Зростання Складності (EnemyDifficultyController)

**Файл:** `src/systems/EnemyDifficultyController.js`

**Опис:** Контролює зростання складності через періодичне додавання нових ворогів.

**Механіка:**
- Кожні **2.5 хвилини** спавниться **+2 нових ворога**
- Максимум: **20 ворогів** (soft cap)
- Показує жартівливі повідомлення про підкріплення

**Приклади повідомлень:**
- *"ТЦК викликали підкріплення. Схоже, ти їм не подобаєшся."*
- *"О, ні. Приїхали нові "фахівці з папірців"."*

**Конфігурація:** `GAME_CONFIG.DIFFICULTY`

---

### 3️⃣ Механіка Госпіталізації Ворогів (Hospital Mechanic)

**Файли:** 
- `src/entities/Chaser.js` (додано `triggerFall()` та `hospitalize()`)
- `src/entities/Car.js` (оновлено для виклику `triggerFall()`)

**Опис:** Вороги виходять з гри після 4 ударів машинами.

**Механіка:**
- Кожен ворог має лічильник `timesHitByCar`
- Після **4 ударів** ворог госпіталізується
- Показується повідомлення
- Ворог видаляється з гри (fade out анімація)

**Приклади повідомлень:**
- *"Один працівник ТЦК поїхав у лікарню. Часто дивився не на дорогу."*
- *"ТЦК мінус один. Машина — сильніша за повістку."*

**Конфігурація:** `GAME_CONFIG.HOSPITAL`

---

### 4️⃣ Прогресія Економіки (MoneyDropController)

**Файл:** `src/systems/MoneyDropController.js`

**Опис:** Контролює прогресію економіки через зміну шансів випадіння купюр.

**Механіка:**
- Використовує **вагову таблицю** для випадіння номіналів
- Кожні **2.5 хвилини** збільшує ваги великих купюр
- Після досягнення максимуму розблоковує **нові номінали** (200₴, 500₴)

**Початкові ваги:**
- 10₴: вага 4 (базовий)
- 20₴: вага 3
- 50₴: вага 2
- 100₴: вага 1

**Нові номінали:**
- 200₴ (розблоковується пізніше)
- 500₴ (розблоковується пізніше)

**Конфігурація:** `GAME_CONFIG.MONEY_PROGRESSION`

---

### 5️⃣ Money Multiplier Pickup (x5)

**Файли:**
- `src/entities/MoneyMultiplierPickup.js`
- `src/systems/MoneyMultiplierController.js`

**Опис:** Спеціальний пікап що дає x5 множник до всіх грошей.

**Механіка:**
- З'являється кожні **5 хвилин**
- Існує **30 секунд**
- Якщо підібрано: **x5 до всіх грошей** на **30 секунд**
- Якщо не підібрано: зникає

**Повідомлення:**
- За 25 секунд: *"Чутки кажуть, скоро буде щось дуже вигідне…"*
- При появі: *"ОГО! Грошовий шанс з'явився. Бігом!"*
- При зборі: *"x5 до всіх грошей на 30 секунд! Грай сміливо!"*
- При зникненні: *"Не встиг? ТЦК теж колись не встигає. Наступного разу."*

**Відображення:**
- Показується на **мінімапі** (золота зірка що пульсує)
- Показується в **HUD** коли активний

**Конфігурація:** `GAME_CONFIG.MONEY_MULTIPLIER`

---

### 6️⃣ Бонус "Вертушка" (Spinner Bonus)

**Файли:**
- `src/entities/bonuses/SpinnerBonus.js`
- `src/scenes/ShopScene.js` (магазин)
- `src/systems/SaveSystem.js` (збереження кількості)

**Опис:** Активний бонус на клавішу **E** що відкидає та заморожує ворогів. **Купується в магазині.**

**Механіка:**
- Активація: клавіша **E**
- Радіус дії: **150 пікселів**
- Відкидає всіх ворогів в радіусі
- Заморожує їх на **4 секунди**
- Cooldown: **45 секунд**
- **Ціна в магазині:** $50 за 1 вертушку
- **Початкова кількість:** 0 (треба купити)

**Магазин:**
- Доступний з ResultScene (кнопка "МАГАЗИН")
- Показує баланс та кількість вертушок
- Кнопка купівлі з перевіркою балансу
- Збереження покупок в localStorage

**Візуальний ефект:**
- Кільце що розширюється від гравця
- Додаткові кільця для ефекту

**Повідомлення:**
- При активації: *"ВЕРТУШКА! Вороги відкинуті!"*
- При cooldown: *"Вертушка ще не готова. Зачекай {seconds} сек."*
- Якщо немає ворогів: *"Поблизу немає ворогів."*
- Якщо немає вертушок: *"У тебе немає вертушок! Купи в магазині."*

**Відображення в HUD:**
- Показує cooldown: *"🌀 Вертушка: 15с (x3)"*
- Коли готова: *"🌀 Вертушка: ГОТОВА (E) x3"* (зелений колір)
- Коли немає: *"🌀 Вертушка: НЕМАЄ (купи в магазині)"* (червоний колір)

**Конфігурація:** `GAME_CONFIG.SPINNER_BONUS`

---

## 🎨 UI Зміни

### HUD (src/ui/HUD.js)

**Додано:**
1. **Money Multiplier індикатор**
   - Показується коли множник активний
   - Текст: *"💰 x5 МНОЖНИК АКТИВНИЙ!"*
   - Пульсує для привернення уваги

2. **Spinner Cooldown індикатор**
   - Показує cooldown вертушки
   - Зелений колір коли готова
   - Текст: *"🌀 Вертушка: ГОТОВА (E)"*

### Minimap (src/ui/Minimap.js)

**Додано:**
- Індикатор Money Multiplier пікапа
- Золота зірка що пульсує
- Оновлюється кожен кадр

---

## ⚙️ Конфігурація

Всі нові механіки налаштовуються через `src/config/gameConfig.js`:

### DIFFICULTY
```javascript
REINFORCEMENT_INTERVAL: 150000,  // 2.5 хвилини
ENEMIES_PER_WAVE: 2,
MAX_ENEMIES: 20,
MESSAGES: [...]
```

### HOSPITAL
```javascript
HITS_TO_HOSPITALIZE: 4,
MESSAGES: [...]
```

### MONEY_PROGRESSION
```javascript
SCALING_INTERVAL: 150000,  // 2.5 хвилини
WEIGHT_INCREMENT: 1,
MAX_WEIGHT: 5,
INITIAL_WEIGHTS: {...},
NEW_DENOMINATIONS: [...]
```

### MONEY_MULTIPLIER
```javascript
SPAWN_INTERVAL: 300000,  // 5 хвилин
LIFETIME: 30000,         // 30 секунд
MULTIPLIER: 5,
DURATION: 30000,         // 30 секунд
MESSAGES: {...}
```

### SPINNER_BONUS
```javascript
KEY: 'KeyE',
COOLDOWN: 45000,         // 45 секунд
PUSH_FORCE: 300,
FREEZE_DURATION: 4000,   // 4 секунди
RADIUS: 150,
SHOP_PRICE: 50,          // Ціна в магазині ($)
INITIAL_COUNT: 0,        // Початкова кількість
MESSAGES: {...}
```

### NOTIFICATIONS
```javascript
DURATION: 4000,          // 4 секунди
FADE_DURATION: 500,
MAX_QUEUE: 5,
Y_POSITION: 100,
PRIORITY: {...}
```

---

## 🔄 Інтеграція в GameScene

Всі системи інтегровані в `src/scenes/GameScene.js`:

### Ініціалізація (create)
```javascript
this.notificationManager = new NotificationManager(this);
this.enemyDifficultyController = new EnemyDifficultyController(this, this.notificationManager);
this.moneyDropController = new MoneyDropController(this, this.notificationManager);
this.moneyMultiplierController = new MoneyMultiplierController(this, this.notificationManager);
this.spinnerBonus = new SpinnerBonus(this, this.player);
```

### Оновлення (update)
```javascript
this.spinnerBonus.update(delta);
this.moneyMultiplierController.update(delta);
```

### Події
```javascript
this.events.on('spawn-reinforcement', ...);
this.events.on('enemy-hospitalized', ...);
this.events.on('money-multiplier-activated', ...);
this.events.on('money-multiplier-deactivated', ...);
```

### Пауза/Відновлення
```javascript
// pauseGame()
this.enemyDifficultyController.pause();
this.moneyDropController.pause();
this.moneyMultiplierController.pause();
this.spinnerBonus.pause();

// resumeGame()
this.enemyDifficultyController.resume();
this.moneyDropController.resume();
this.moneyMultiplierController.resume();
this.spinnerBonus.resume();
```

---

## 📊 Статистика Змін

**Створено нових файлів:** 6
- NotificationManager.js
- EnemyDifficultyController.js
- MoneyDropController.js
- MoneyMultiplierController.js
- MoneyMultiplierPickup.js
- SpinnerBonus.js

**Оновлено файлів:** 8
- gameConfig.js (додано 6 нових секцій)
- Chaser.js (Hospital Mechanic)
- Car.js (виклик triggerFall для ворогів)
- GameScene.js (інтеграція всіх систем)
- HUD.js (нові індикатори)
- Minimap.js (індикатор Money Multiplier)
- ShopScene.js (магазин з купівлею вертушок)
- SaveSystem.js (збереження кількості вертушок)

**Загальна кількість рядків коду:** ~1500+

---

## 🎯 Результат

Гра тепер має:
- ✅ Динамічну складність що зростає
- ✅ Прогресію економіки з новими номіналами
- ✅ Спеціальні події (Money Multiplier)
- ✅ Активні бонуси (Вертушка)
- ✅ Систему повідомлень для всіх подій
- ✅ Покращений feedback для гравця
- ✅ Більше різноманітності та непередбачуваності

Гра стала **менш одноманітною** та **постійно еволюціонує**! 🎉
